name: 01. Sentinel Scout (Frontline Scan)

on:
  schedule:
    - cron: '0 0,12 * * *' # 北京时间 8:00 和 20:00
  workflow_dispatch:

jobs:
  scout_mission:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 必须赋予写权限以提交数据

    steps:
      - name: 📍 Checkout Frontline
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Install Dependencies
        # ✅ 修正：同时安装 axios
        run: npm install @octokit/rest axios --no-fund

      # Phase 1: 执行代码侦察 (GitHub)
      - name: 📡 Run Sentinel Scan (GitHub)
        run: node sentinel.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}

      # Phase 1.5: 执行论文侦察 (Science Radar)
      # ✅ 新增：执行论文扫描并注入邮箱
      - name: 📜 Run Sentinel Scan (Science Radar)
        run: node paper_sentinel.js
        env:
          CONTACT_EMAIL: ${{ secrets.CONTACT_EMAIL }}

      # Phase 2: 暂存情报 (提交到当前仓库)
      - name: 💾 Commit Intel to Frontline
        run: |
          git config --global user.name 'Sentinel Scout'
          git config --global user.email 'scout@architect.alpha'
          
          # 检查 data/ 目录下是否有任何新数据 (包含 GitHub 和 Papers)
          if [[ -n $(git status --porcelain data/) ]]; then
            git add data/
            git commit -m "Intel: New signals captured [$(date +%F)]"
            git push
          else
            echo "No new intel found, skipping commit." 
          fi
